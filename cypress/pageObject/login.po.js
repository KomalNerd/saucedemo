class loginpo {
  selector = {
    inputbox_username: '.input_error.form_input[name="user-name"]',
    inputbox_password: '.input_error.form_input[name="password"]',
    button_submit: "#login-button",
  };
}

export default new loginpo();
